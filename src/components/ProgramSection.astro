---
interface Props {
  title: string;
  description: string;
  image?: string;
  imageAlt?: string;
  reverse?: boolean;
  rotate?: number;
}

const { title, description, image, imageAlt, reverse = false, rotate = 0 } = Astro.props;
---

<div class={`program-section ${reverse ? 'program-section--reverse' : ''}`}>
  {image && (
    <div class="program-image-wrap" style={`--rotate: ${rotate}deg; --hover-deg: ${reverse ? '3deg' : '-4deg'}`}>
      <img
        src={image}
        alt={imageAlt ?? title}
        class="program-image"
        loading="lazy"
        decoding="async"
      />
    </div>
  )}
  <div class="program-content">
    <h3>{title}</h3>
    <p>{description}</p>
    <slot />
  </div>
</div>

<style>
  .program-section {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    margin-bottom: 5rem;
  }

  .program-section:last-child {
    margin-bottom: 0;
  }

  .program-section--reverse {
    direction: rtl;
  }

  .program-section--reverse > * {
    direction: ltr;
  }

  .program-image-wrap {
    border-radius: var(--radius-card);
    overflow: hidden;
    aspect-ratio: 4 / 3;
    transform: rotate(var(--rotate)) scale(0.9);
    transition: transform 0.5s ease, box-shadow 0.5s ease;
  }

  .program-section:hover .program-image-wrap {
    transform: rotate(calc(var(--rotate) + var(--hover-deg))) scale(1.0);
    box-shadow: 0 16px 40px rgba(11, 37, 69, 0.15);
  }

  .program-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
  }

  .program-section:hover .program-image {
    transform: scale(1.05);
  }

  .program-content h3 {
    font-size: clamp(1.25rem, 2.5vw, 1.625rem);
    margin-bottom: 1rem;
  }

  .program-content p {
    color: var(--color-text-secondary);
    font-size: 1rem;
    line-height: 1.7;
  }

  @media (max-width: 768px) {
    .program-section,
    .program-section--reverse {
      grid-template-columns: 1fr;
      direction: ltr;
      gap: 2rem;
    }
  }
</style>
